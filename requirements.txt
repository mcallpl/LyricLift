Flask==2.3.3
Werkzeug==2.3.7
gunicorn==21.2.0
certifi>=2024.2.2

# Audio processing tools invoked as subprocesses (python3 -m yt_dlp / -m whisper).
# openai-whisper is heavy — it pulls in torch (~2GB). yt-dlp is intentionally
# left unpinned: keep it current, since YouTube extraction breaks often.
# NOTE: both also require ffmpeg, a SYSTEM package (apt install ffmpeg).
yt-dlp
openai-whisper
